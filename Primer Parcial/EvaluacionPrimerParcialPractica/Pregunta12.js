let pais = ['Ecuador', 'España', 'Argentina', 'Chile', 'Colombia', 'Venezuela', 'Perú', 'Costa Rica'];

function imprimirElementosArray(array) {
    for (let i = 0; i < array.length; i++) {
      console.log(array[i]);
    }
  }

imprimirElementosArray(pais);